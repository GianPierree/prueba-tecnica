service: tokens

useDotenv: true

provider: 
  name: aws
  runtime: nodejs16.x

functions:
  token:
    handler: src/tokens.handler
    events: 
      - httpApi: POST /tokens

custom:
  esbuild:
    bundle: true
    minify: true

plugins: 
  - serverless-offline
  - serverless-esbuild